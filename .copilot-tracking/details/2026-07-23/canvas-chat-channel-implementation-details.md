<!-- markdownlint-disable-file -->
# Implementation Details: Canvas Chat Channel Implementation

## Context Reference

Sources: .copilot-tracking/research/2026-07-23/canvas-chat-feature-research.md, .copilot-tracking/research/subagents/2026-07-23/codebase-chat-integration-research.md, .copilot-tracking/research/subagents/2026-07-23/chat-library-options-research.md, /memories/repo/startup-db-ordering.md

## Implementation Phase 1: Contracts and Persistence Foundation

<!-- parallelizable: false -->

### Step 1.1: Lock v1 chat constraints before implementation

Finalize and record concrete values for `retentionDays`, `maxMessageLength`, `replayPageSize`, `maxReplayPageSize`, and `ackTimeoutMs` in a dedicated server configuration module or constants section. Reference these values from validators and replay handlers rather than hardcoding duplicates in multiple locations.

Files:
* apps/server/src/index.ts - Add centralized constants or config mapping used by handlers
* apps/server/src/contracts.ts - Align reason codes and payload limits with finalized constraints

Success criteria:
* All five v1 constraint values are explicitly defined before handler implementation
* Validators, replay pagination, and ack behavior reference the same constraints source

Context references:
* .copilot-tracking/research/2026-07-23/canvas-chat-feature-research.md (Lines 177-188) - Suggested v1 chat config block
* .copilot-tracking/research/2026-07-23/canvas-chat-feature-research.md (Lines 336-341) - Throughput and reconnect sizing guidance

Dependencies:
* None

### Step 1.2: Add chat event and payload types to shared contracts

Define `ChatMessage`, `SendChatMessagePayload`, `SendChatMessageAck`, `RequestChatReplayPayload`, and server event payloads in the shared contract file. Extend `ClientToServerEvents` and `ServerToClientEvents` with chat send, receive, and replay events. Ensure naming aligns with existing snake_case socket event conventions.

Files:
* apps/server/src/contracts.ts - Add chat domain types and socket event signatures

Success criteria:
* Shared contract file compiles with new chat types and event signatures
* Both server and client can import chat payload types from one source of truth

Context references:
* .copilot-tracking/research/subagents/2026-07-23/codebase-chat-integration-research.md (Lines 88-120) - Contract extension guidance
* .copilot-tracking/research/2026-07-23/canvas-chat-feature-research.md (Lines 72-105) - Example chat type definitions

Dependencies:
* Step 1.1 completion

### Step 1.3: Add Postgres chat schema and migration for message storage, ordering, and idempotency

Create a `chat_messages` table in Drizzle schema with fields for `canvas_id`, `sender_client_id`, `text`, `server_seq`, `client_message_id`, and server timestamp. Add migration SQL and indexes: unique `(canvas_id, server_seq)` for replay ordering and unique `(canvas_id, sender_client_id, client_message_id)` for idempotency when `client_message_id` is present.

Files:
* apps/server/src/db/schema.ts - Add table and index definitions
* apps/server/migrations/<timestamp>_chat_messages.sql - Create table and constraints
* apps/server/migrations/meta/ - Update migration metadata files generated by toolchain

Success criteria:
* Migration applies cleanly in local development and CI bootstrap flows
* Query patterns for replay by canvas and sequence are index-backed

Context references:
* .copilot-tracking/research/2026-07-23/canvas-chat-feature-research.md (Lines 157-200) - Persistence strategy recommendation
* /memories/repo/startup-db-ordering.md (Lines 1-2) - Startup and test sequencing caveat

Dependencies:
* Step 1.2 completion

### Step 1.4: Validate phase changes

Run lint and build commands for files modified in this phase. Skip validation when it conflicts with parallel phases running the same validation scope.

Validation commands:
* npm --prefix apps/server run lint - Server TypeScript and style checks
* npm --prefix apps/server run build - Server compile validation

## Implementation Phase 2: Server Chat Event Pipeline

<!-- parallelizable: false -->

### Step 2.1: Implement runtime chat payload validators and guardrails in server handler layer

Add chat-specific payload guards beside existing validator helpers. Enforce non-empty text, maximum message length, and `payload.canvasId === socket.data.sessionId` invariant for send and replay requests. Reject invalid payloads with structured ack reasons matching shared contract unions.

Files:
* apps/server/src/index.ts - Add validation helpers and authorization checks

Success criteria:
* Invalid payloads are rejected deterministically and do not broadcast
* Cross-canvas payload attempts fail with explicit rejection reasons

Context references:
* .copilot-tracking/research/subagents/2026-07-23/codebase-chat-integration-research.md (Lines 72-87) - Existing guard pattern references
* .copilot-tracking/research/2026-07-23/canvas-chat-feature-research.md (Lines 126-156) - Channel isolation scenario

Dependencies:
* Step 1.1 completion

### Step 2.2: Implement send and replay handlers with room fanout, ack semantics, and replay bounds

Implement `send_chat_message` handler to persist message row, assign/return `serverSeq`, and emit `chat_message` to `io.to(sessionId)`. Implement `request_chat_replay` handler to query ordered rows after offset with server-enforced pagination caps and emit `chat_replay` to requester socket only.

Files:
* apps/server/src/index.ts - Add handler wiring in connection lifecycle
* apps/server/src/db/ - Add repository/query helper module if current code structure requires extraction

Success criteria:
* Send flow returns accepted ack only after persistence succeeds
* Replay flow returns ordered bounded messages and next offset metadata

Context references:
* .copilot-tracking/research/2026-07-23/canvas-chat-feature-research.md (Lines 126-156) - Room and replay implementation path
* .copilot-tracking/research/subagents/2026-07-23/codebase-chat-integration-research.md (Lines 20-44) - Existing room fanout patterns

Dependencies:
* Step 1.2 completion
* Step 2.1 completion

### Step 2.3: Add server integration tests for per-canvas isolation, replay ordering, and idempotency

Add integration tests that assert: messages are only received by clients in the same canvas/session room, replay ordering by `serverSeq` is monotonic, and duplicate client retries with same `clientMessageId` produce idempotent outcomes.

Files:
* apps/server/src/index.integration.test.ts - Add end-to-end socket scenarios
* apps/server/src/index.test.ts - Add focused handler tests where appropriate

Success criteria:
* New integration scenarios fail before implementation and pass after
* Test assertions explicitly cover cross-canvas isolation and dedupe behavior

Context references:
* .copilot-tracking/research/subagents/2026-07-23/codebase-chat-integration-research.md (Lines 189-209) - Risk and pitfall list

Dependencies:
* Step 2.2 completion

### Step 2.4: Add chat observability instrumentation for reliability and throughput

Instrument the server chat pipeline with counters and timing measurements for send failures, ack latency, replay lag, and per-canvas message volume. Ensure metrics are emitted through the project's existing logging or monitoring hooks and are queryable during smoke validation.

Files:
* apps/server/src/index.ts - Add metric emission points in send/replay handlers and error paths
* apps/server/src/startup/ - Wire counters into existing telemetry bootstrap if required by current architecture
* apps/server/src/index.integration.test.ts - Add assertions or smoke checks that metrics/log markers are emitted

Success criteria:
* Send failures and rejected acks increment observable counters
* Replay responses emit lag or page-size observability markers

Context references:
* .copilot-tracking/research/2026-07-23/canvas-chat-feature-research.md (Line 343) - Counter guidance

Dependencies:
* Step 2.2 completion

### Step 2.5: Validate phase changes

Run lint and build commands for files modified in this phase. Skip validation when it conflicts with parallel phases running the same validation scope.

Validation commands:
* npm --prefix apps/server run lint - Server lint scope
* npm --prefix apps/server run test - Server test suite

## Implementation Phase 3: Client Networking and Chat UI

<!-- parallelizable: false -->

### Step 3.1: Extend socket hook API with chat listeners and emit actions

Add optional callback props for `onChatMessage` and `onChatReplay`, register listeners in `useSocketConnection`, and expose emit helpers for `send_chat_message` and `request_chat_replay` through existing socket action references.

Files:
* apps/client/src/network/useSocketConnection.ts - Hook signature and listener setup

Success criteria:
* Chat events are subscribed and unsubscribed with existing hook lifecycle
* App layer can emit send and replay requests through typed actions

Context references:
* .copilot-tracking/research/subagents/2026-07-23/codebase-chat-integration-research.md (Lines 133-151) - Hook integration points

Dependencies:
* Step 1.1 completion

### Step 3.2: Add chat state management and replay request flow in App-level orchestration

Introduce chat message state keyed by current session/canvas, dispatch replay request on socket connect and/or canvas enter, merge incoming replay and live messages, and route outbound sends through socket action callbacks.

Files:
* apps/client/src/App.tsx - Chat state, event handlers, and action plumbing

Success criteria:
* Switching canvases/session contexts resets or re-keys chat state correctly
* Reconnect path requests and merges replay without duplicating live messages

Context references:
* .copilot-tracking/research/subagents/2026-07-23/codebase-chat-integration-research.md (Lines 45-71) - Session/canvas identity flow
* .copilot-tracking/research/2026-07-23/canvas-chat-feature-research.md (Lines 106-125) - Delivery and replay requirements

Dependencies:
* Step 3.1 completion

### Step 3.3: Implement chat panel UI component and integrate into existing layout

Create a dedicated chat panel component under the UI folder. Render message list, timestamp/client attribution, input composer, and send action feedback for rejected acks. Integrate component into existing layout without regressing current controls/presence workflows.

Files:
* apps/client/src/ui/ChatPanel.tsx - New chat UI component
* apps/client/src/ui/ChatPanel.css - Scoped styling for message list and input states
* apps/client/src/App.tsx - Component integration and props wiring

Success criteria:
* Chat panel sends and receives messages in active canvas context
* UI remains usable on desktop and mobile breakpoints used by current app

Context references:
* .copilot-tracking/research/subagents/2026-07-23/chat-library-options-research.md (Lines 27-109) - UI kit versus backend trade-offs

Dependencies:
* Step 3.2 completion

### Step 3.4: Add client tests for listener wiring and message rendering behavior

Add tests for `useSocketConnection` chat callbacks and App/UI message rendering, including replay merge and duplicate suppression behavior.

Files:
* apps/client/src/network/useSocketConnection.test.ts - Listener and emit contract tests
* apps/client/src/App.test.tsx - Chat rendering and send interaction tests

Success criteria:
* Client tests verify chat callbacks register, fire, and clean up correctly
* UI test cases cover send flow and replay insertion ordering

Context references:
* .copilot-tracking/research/subagents/2026-07-23/codebase-chat-integration-research.md (Lines 211-219) - Recommended code-touch list and tests

Dependencies:
* Step 3.3 completion

### Step 3.5: Validate phase changes

Run lint and build commands for files modified in this phase. Skip validation when it conflicts with parallel phases running the same validation scope.

Validation commands:
* npm --prefix apps/client run lint - Client lint scope
* npm --prefix apps/client run test - Client test suite
* npm --prefix apps/client run build - Client build scope

## Implementation Phase 4: Validation

<!-- parallelizable: false -->

### Step 4.1: Run full project validation

Execute all validation commands for the project:
* npm run lint
* npm --prefix apps/server run test
* npm --prefix apps/client run test
* npm --prefix apps/server run build
* npm --prefix apps/client run build

### Step 4.2: Fix minor validation issues

Iterate on lint errors, build warnings, and test failures. Apply fixes directly when corrections are straightforward and isolated.

### Step 4.3: Report blocking issues

When validation failures require changes beyond minor fixes:
* Document the issues and affected files.
* Provide the user with next steps.
* Recommend additional research and planning rather than inline fixes.
* Avoid large-scale refactoring within this phase.

### Step 4.4: Validate scale-readiness assumptions and observability outputs

Confirm operational assumptions documented in research are represented in deliverables:
* Sticky-session ingress requirement is documented for multi-instance deployments.
* Replay source-of-truth remains Postgres regardless of Socket.IO adapter choice.
* Observability signals added in Step 2.4 are visible in integration or smoke validation output.

## Implementation Phase 5: Review-Driven Rework

<!-- parallelizable: false -->

### Step 5.1: Correct server idempotency rebroadcast and ack reason semantics

Update the chat send handler so idempotent retries return accepted acks without broadcasting duplicate `chat_message` events to the room. Add a distinct reject reason for persistence failures instead of mapping DB failures to `INVALID_PAYLOAD`.

Files:
* apps/server/src/contracts.ts - Add explicit server failure reason for send ack union
* apps/server/src/index.ts - Skip fanout when `result.idempotent` is true; return explicit DB failure ack reason
* apps/server/src/index.integration.test.ts - Add/replace tests validating no duplicate broadcast on idempotent retry and correct reject reason behavior

Success criteria:
* Idempotent retries do not produce duplicate room messages
* DB/persistence failures return a distinct non-payload error reason

### Step 5.2: Enforce chat retentionDays in retention cleanup path

Extend retention pruning to include chat message deletion based on `CHAT_CONFIG.retentionDays`, and wire the retention job to pass the chat retention cutoff.

Files:
* apps/server/src/db/repository.ts - Add chat retention pruning in `pruneRetention`
* apps/server/src/jobs/retention.ts - Pass chat retention cutoff derived from chat config
* apps/server/src/jobs/retention.test.ts - Assert chat cutoff is provided to prune function

Success criteria:
* `CHAT_CONFIG.retentionDays` is reflected in runtime retention behavior
* Retention job tests cover chat retention arguments

### Step 5.3: Improve client replay continuity for pagination and reconnect

Implement replay pagination continuation using `hasMore` and `nextAfterSeq`, and ensure replay is requested after connection becomes available, including reconnect scenarios where `sessionId` remains unchanged.

Files:
* apps/client/src/App.tsx - Add replay continuation loop and reconnect-safe replay request triggers

Success criteria:
* Multi-page replay is fetched until complete
* Replay request is not silently lost when socket connection is unavailable at initial session entry

### Step 5.4: Add user-visible chat send rejection feedback and fix panel layout integration

Track last chat send failure in app state and surface a visible status/error message in chat UI. Align chat panel placement with layout selectors so side-panel behavior is consistent with CSS.

Files:
* apps/client/src/App.tsx - Store/send rejection feedback state and pass to chat panel
* apps/client/src/ui/ChatPanel.tsx - Render send rejection feedback region
* apps/client/src/ui/ChatPanel.css - Add styling for status/error state
* apps/client/src/App.css - Ensure panel integration selector targets actual rendered structure

Success criteria:
* Rejected sends show user-visible feedback
* Chat panel layout styles apply consistently in canvas view

### Step 5.5: Replace descriptive server chat tests with behavioral socket-backed assertions

Replace documentation-style chat tests with executable behavior tests in integration scope, including isolation, idempotency non-rebroadcast, replay ordering, and replay pagination behavior.

Files:
* apps/server/src/index.integration.test.ts - Implement real runtime behavior tests; remove descriptive placeholder cases

Success criteria:
* Chat integration assertions exercise actual server runtime behavior
* Test coverage directly proves review findings are resolved

### Step 5.6: Add chat-specific client tests for networking and UI behavior

Add tests for chat listener lifecycle in socket hook and chat UI/replay behavior in app tests, including reconnect replay trigger and user-visible rejection feedback.

Files:
* apps/client/src/network/useSocketConnection.test.ts - Add chat listener subscribe/unsubscribe tests
* apps/client/src/App.test.tsx - Add chat replay continuation/reconnect and rejection feedback tests

Success criteria:
* Client chat-specific tests exist in the required files and pass
* Tests cover replay merge/continuity and rejection visibility

### Step 5.7: Validate rework and observability proof

Run project validation and ensure observability markers are asserted in test output for chat send and replay paths.

Validation commands:
* npm run lint
* npm --prefix apps/server run test
* npm --prefix apps/client run test
* npm run build

## Dependencies

* Root workspace Node package scripts and lockfile consistency
* Server migration tooling and DB availability for integration tests

## Success Criteria

* End-to-end per-canvas chat flow works with existing Socket.IO transport and room model
* Replay and idempotency behavior are validated through automated tests
* Validation commands pass for modified client and server scopes or blockers are documented
